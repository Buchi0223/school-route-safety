# 通学路探検ツール MSRH機能 仕様書

## 1. 概要

### 1.1 背景と課題

Honda Safety Mapには多くの危険地点投稿情報が存在するが、以下の課題がある：

- 投稿情報が多すぎてナビ配信時にノイズになる
- 主観的な投稿が検証されていない
- 投稿が1way（投稿して終わり）になっている

### 1.2 解決アプローチ

**MSRH（My School Route Hazard）ピン**という新しいデータ層を導入する。

- 親子が通学路上で「危険だと思う場所」に自らピンを立てる
- これにより投稿情報に対する「検証」データが蓄積される
- 複数の親子が同じ場所にピンを立てることで客観性が担保される

### 1.3 ビジネスモデル

- 通学路探検ツールへのQRコード付きギフトカードをPTAに販売
- 卒園・入学時に配布
- 学校ごとに識別子とパスワードを発行

---

## 2. ユーザーフロー

```
1. カードのQRコードを読み取り
2. 学校ID・パスワードを入力してアクセス
3. 通学路を設定（出発地 → 学校）
4. 地図上に既存の投稿情報が表示される
5. 投稿情報を参考にしながら、自分が危険だと思う場所にMSRHピンを立てる
6. MSRHピンを立てた場所でバーチャルツアー＆KYT体験
7. （任意）新たに気づいた危険をSafety Mapに投稿
```

---

## 3. データ構造

### 3.1 School（学校マスタ）

| 項目 | 型 | 必須 | 備考 |
|------|-----|------|------|
| school_id | string | ○ | 一意識別子 |
| password | string | ○ | アクセス用パスワード（ハッシュ化保存） |
| name | string | ○ | 学校名 |
| location | object | ○ | 学校の位置（lat, lng） |
| created_at | datetime | ○ | 登録日時 |

### 3.2 Route（通学路）

| 項目 | 型 | 必須 | 備考 |
|------|-----|------|------|
| route_id | string | ○ | UUID |
| school_id | string | ○ | 学校識別子（FK） |
| start_point | object | ○ | 出発地点（lat, lng） |
| end_point | object | ○ | 到着地点（lat, lng）※通常は学校位置 |
| path | array | ○ | 経路の座標列 [{lat, lng}, ...] |
| session_id | string | ○ | セッション識別子 |
| created_at | datetime | ○ | 作成日時 |

### 3.3 MSRH_Pin（MSRHピン）

| 項目 | 型 | 必須 | 備考 |
|------|-----|------|------|
| id | string | ○ | UUID |
| lat | number | ○ | 緯度 |
| lng | number | ○ | 経度 |
| school_id | string | ○ | 学校識別子（FK） |
| route_id | string | ○ | 通学路ID（FK） |
| reason | enum | ○ | 危険理由（下記参照） |
| session_id | string | ○ | セッション識別子（重複防止用） |
| created_at | datetime | ○ | 作成日時 |

#### reason（危険理由）のenum値

| 値 | 表示名 |
|----|--------|
| narrow_road | 道路が狭い/歩道がない |
| poor_visibility | 見通しが悪い |
| speeding_cars | スピードが出ているクルマが多い |
| pedestrian_rushout | 歩行者/自転車の飛出しが多い |
| other | その他 |

※ Safety Map投稿情報と同じ分類を採用

---

## 4. 機能仕様

### 4.1 認証

- 学校ID + パスワードでアクセス
- 個人情報は取得しない
- セッションIDはブラウザセッション単位で生成

### 4.2 通学路設定

- 出発地点：ユーザーが地図上で指定
- 到着地点：学校位置（school_idから自動取得）または手動指定
- 経路：ルート計算APIで算出、または手動で描画

### 4.3 MSRHピン設置

#### 表示
- 地図上にSafety Map投稿情報を表示（参考情報として）
- 自分が立てたMSRHピンは別アイコンで表示

#### 設置操作
- 地図上をタップ/クリックしてピンを設置
- 設置時に危険理由を選択（必須）

#### 重複防止ロジック
- 同一セッション内で、既存のMSRHピンから半径○m以内に新規ピンを立てようとした場合：
  - 新規作成ではなく、既存ピンの**移動**として扱う
  - 理由は再選択可能

### 4.4 バーチャルツアー

- MSRHピンを立てた地点のみで停止
- 停止時にStreet Viewを表示
- KYT（危険予測トレーニング）として、どのような危険があるか確認

---

## 5. 投稿情報とのマッチング（将来機能）

### 5.1 目的

MSRHピンの集積データを用いて、Safety Map投稿情報をフィルタリングする。

### 5.2 マッチング条件（案）

- 投稿情報の位置から半径○m以内にMSRHピンが存在するか
- reasonが一致するか（オプション）

### 5.3 フィルタ閾値（案）

- MSRHピンが○件以上集まった投稿情報を「検証済み」とする
- 「検証済み」情報をナビ配信チームに提供

---

## 6. 未決定事項

| 項目 | 選択肢 | 備考 |
|------|--------|------|
| session_idの寿命 | ブラウザセッション / 1日 / 無期限 | 重複防止の厳密さに影響 |
| 重複防止の距離閾値 | 10m / 20m / 50m | 要検証 |
| 投稿情報マッチングの距離閾値 | 20m / 50m / 100m | 要検証 |
| フィルタ閾値（何件で検証済み） | 3件 / 5件 / 10件 | データ蓄積後に調整 |
| 理由は単一選択か複数選択か | 単一 / 複数 | UI複雑度とデータ精度のトレードオフ |

---

## 7. 今後の検討事項

- 毎年の更新フロー（同じ親子が翌年も利用する場合）
- 投稿情報が存在しない場所へのMSRHピンの扱い（新規危険発見）
- KYT体験の具体的なUIデザイン
- データ分析ダッシュボード（学校・自治体向け）

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-01-23 | 初版作成 |
