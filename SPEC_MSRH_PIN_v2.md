# マイ危険ポイント機能 仕様書

> **バージョン**: v0.6 (デモ版)
> **ステータス**: ドラフト
> **対象**: 通学路探検ツール デモアプリケーション

---

## 1. 概要

### 1.1 背景と目的

Honda Safety Mapには多くの危険地点投稿情報（HazardPoint）が存在するが、以下の課題がある：

- 投稿情報が多すぎてナビ配信時にノイズになる
- 主観的な投稿が検証されていない
- 投稿が1way（投稿して終わり）になっている

**マイ危険ポイント**は、親子が通学路上で「危険だと思う場所」に自らピンを立てることで、投稿情報の「検証」データを蓄積する仕組み。

### 1.2 デモ版の範囲

本仕様書は**デモ版**として、以下の範囲を対象とする：

| 項目 | デモ版 | 将来版 |
|------|--------|--------|
| 認証 | なし（匿名利用） | 学校ID + パスワード |
| データ保存 | ローカルストレージ | バックエンドAPI |
| セッション | ブラウザセッション | サーバー管理 |
| マッチング分析 | なし | 投稿情報との照合 |

### 1.3 既存機能との関係

アプリのトップレベルに**2つの独立した機能ボタン**を配置する：

```
アプリ トップ画面
│
├── [マイ危険ポイント設置] ボタン ← 新規追加
│   └── 地図上でマイ危険ポイントを設置・管理
│
└── [探検モード] ボタン ← 既存（拡張）
    ├── 経路設定
    ├── 巡回対象の選択 ← [NEW]
    │   ├── マイ危険ポイント（自分設置）
    │   └── Safety Map投稿情報（自動選別）
    └── Street Viewバーチャルツアー
```

**ポイント**:
- マイ危険ポイント設置と探検モードは**完全に独立**した機能
- 探検モード開始時に「何を巡るか」を選択する

---

## 2. ユーザーフロー

### 2.1 機能構成

アプリのトップレベルに**2つの独立した機能**を配置：

| ボタン | 機能 | 目的 |
|--------|------|------|
| **マイ危険ポイント設置** | ピン設置モード | 自分が危険だと思う場所を記録 |
| **探検モード** | バーチャルツアー | 経路上の危険地点をStreet Viewで確認 |

### 2.2 マイ危険ポイント設置のフロー

```
1. トップ画面 → 「マイ危険ポイント設置」ボタンをタップ
2. ピン設置画面が開く
   - 地図上に既存の投稿情報（HazardPoint）が参考表示される
3. 地図上をタップ/クリックしてマイ危険ポイントを設置
   - 危険理由を選択（複数選択可）
4. 必要な数だけピンを設置
5. 「完了」→ トップ画面に戻る
```

### 2.3 探検モード（バーチャルツアー）のフロー

```
1. トップ画面 → 「探検モード」ボタンをタップ
2. 通学路を設定（出発地 → 目的地）
3. ★巡回対象を選択：
   ┌─────────────────────────────────────┐
   │  どの情報を使ってツアーしますか？     │
   ├─────────────────────────────────────┤
   │  ○ 自分で立てたピン（マイ危険ポイント）      │
   │     → 設置済み: 5件                 │
   │                                     │
   │  ○ Safety Mapの投稿情報             │
   │     → この経路上: 3件（自動選別）    │
   └─────────────────────────────────────┘
4. ツアー開始 → 選択した地点で自動停止
5. Street Viewで確認 → 次へ進む
6. ツアー完了 → 修了証表示
```

### 2.4 状態管理

#### マイ危険ポイント設置モード

| 状態 | 内容 |
|------|------|
| `pin_idle` | 初期状態（ピン設置待ち） |
| `pin_editing` | ピン編集中 |

#### 探検モード（既存 + 拡張）

| 状態 | 内容 |
|------|------|
| `route_setting` | 経路設定 |
| `target_select` | 【新規】巡回対象選択 |
| `touring` | バーチャルツアー実行 |
| `hazard_stop` | 地点で停止して確認 |
| `completed` | 修了証表示 |

### 2.5 マイ危険ポイント設置画面の詳細

```
┌─────────────────────────────────────────────────────────┐
│  マイ危険ポイント設置                              [完了]       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 地図上に既存HazardPointが参考表示される              │
│                                                         │
│  2. 地図上をタップ/クリック                              │
│     └ 理由選択ダイアログが表示                          │
│     └ 複数の理由を選択可能                              │
│     └ マイ危険ポイントが設置される                              │
│                                                         │
│  3. 設置したピンはいつでも編集・削除可能                 │
│                                                         │
│  4. 「完了」ボタンでトップ画面に戻る                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.6 Safety Map投稿情報の自動選別

バーチャルツアーで「Safety Mapの投稿情報」を選択した場合、経路上のすべてのHazardPointではなく、**重要度の高い3〜5件を自動選別**する。

#### 選別アルゴリズム（案）

```
1. 経路から半径50m以内のHazardPointを抽出

2. 優先度スコアを計算（高い順に選別）：
   - タイプ別重み付け：
     - accident（事故多発）: +10点
     - braking（急ブレーキ）: +8点
     - intersection（見通し悪い交差点）: +6点
     - user_report（ユーザー投稿）: +4点

   - 経路上の位置：
     - 交差点付近: +3点
     - 経路の曲がり角付近: +2点

   - 距離：
     - 経路から近いほど高スコア（10m以内: +2点）

3. 上位3〜5件を選出
   - 最小: 3件（経路が短い場合は全件）
   - 最大: 5件
   - 地点が近接している場合は代表1件のみ
```

#### 選別結果の表示

```
┌─────────────────────────────────────────┐
│  Safety Mapの投稿情報を使います          │
│                                         │
│  この経路で重要な 4地点 を選びました：    │
│                                         │
│  🔴 〇〇交差点（事故多発エリア）         │
│  🟠 △△通り（急ブレーキ多発）           │
│  ⚠️ □□前（見通しの悪い交差点）         │
│  💬 ××付近（ユーザー投稿）             │
│                                         │
│  [この地点でツアー開始]                  │
└─────────────────────────────────────────┘
```

---

## 3. データ構造

### 3.1 マイ危険ポイント（MyHazardPoint）

```typescript
interface MyHazardPoint {
  id: string;                    // UUID
  lat: number;                   // 緯度
  lng: number;                   // 経度
  reasons: MyHazardReason[];         // 危険理由（複数選択可、最低1つ必須）
  reasonDetail?: string;         // 補足説明（任意、「その他」選択時）
  routeId?: string;              // 紐づく経路ID（任意）
  createdAt: string;             // ISO 8601形式
  updatedAt: string;             // ISO 8601形式
}
```

> **Note**: デモ版では既存HazardPointとの紐付け（`nearbyHazardId`）は実装しない。将来版で検討。

### 3.2 危険理由（MyHazardReason）

既存のHazardPointタイプと整合性を持たせつつ、ユーザーが選びやすい表現に：

| 値 | 表示名 | 既存タイプとの対応 |
|----|--------|-------------------|
| `narrow_road` | 道路が狭い/歩道がない | - |
| `poor_visibility` | 見通しが悪い | intersection |
| `speeding_cars` | スピードを出すクルマが多い | braking |
| `high_traffic` | 交通量が多い | accident |
| `pedestrian_conflict` | 歩行者/自転車との接触が心配 | user_report |
| `other` | その他 | - |

### 3.3 ローカルストレージ構造

```typescript
// localStorage key: 'my_hazard_points'
interface MyHazardStorage {
  version: number;               // スキーマバージョン
  pins: MyHazardPoint[];               // ピン一覧
  lastUpdated: string;           // 最終更新日時
}
```

---

## 4. 機能仕様

### 4.1 マイ危険ポイントの表示

#### 4.1.1 アイコン

| 要素 | 仕様 |
|------|------|
| 形状 | 丸型ピン（既存HazardPointと区別） |
| 色 | 紫系（#8B5CF6） |
| サイズ | 24px（通常）/ 32px（選択時） |
| ラベル | 理由に応じたアイコン or 数字 |

#### 4.1.2 地図上の表示

- マイ危険ポイントは既存HazardPointより**前面**に表示
- 選択時は拡大 + 情報ポップアップ表示
- 複数ピンが近接する場合はクラスタリング（将来検討）

### 4.2 マイ危険ポイントの設置

#### 4.2.1 設置操作

```
1. ピン設置フェーズで地図上をタップ/クリック

2. 理由選択ダイアログ表示
   - チェックボックス形式で複数の理由を選択可能
   - 最低1つは選択必須
   - 「その他」選択時は補足テキスト入力欄

3. 「ピンを立てる」ボタンで保存
```

#### 4.2.2 重複防止ロジック

同一ブラウザセッション内で、既存マイ危険ポイントから**半径30m以内**に新規ピンを設置しようとした場合：

| ケース | 動作 |
|--------|------|
| 完全に同じ場所 | 既存ピンの編集を提案 |
| 30m以内 | 警告表示「近くにピンがあります。移動しますか？」 |
| 30m超 | 通常通り新規作成 |

### 4.3 マイ危険ポイントの編集・削除

| 操作 | 方法 |
|------|------|
| 編集 | ピンをタップ → 詳細表示 → 「編集」ボタン |
| 削除 | ピンをタップ → 詳細表示 → 「削除」ボタン |
| 移動 | 編集モード → ドラッグ&ドロップ |

### 4.4 ツアー連携

#### 4.4.1 停止対象

ツアー中に自動停止する地点：

| 種類 | 停止条件 | 表示 |
|------|---------|------|
| HazardPoint（既存） | 経路から30m以内 | 既存のガイド表示 |
| マイ危険ポイント（自分設置） | 経路から30m以内 | 設置時の理由を表示 |

#### 4.4.2 マイ危険ポイント停止時の動作

1. 自動停止（または手動停止）
2. Street Viewで現地の様子を確認
3. 自分が選んだ危険理由を表示
4. 「確認OK」ボタンで次へ進む

#### 4.4.3 停止順序

- HazardPointとマイ危険ポイントは経路上の位置順に停止
- 同一地点に両方ある場合はHazardPointを優先表示し、マイ危険ポイント情報を補足表示

### 4.5 完了画面

ツアー完了後の完了画面（CertificateModal）に以下を追加：

- 今回設置したマイ危険ポイントの数
- 設置したピンの一覧（理由別にグループ化）
- 「ピンを確認する」ボタン（地図に戻って確認）

---

## 5. UI仕様

### 5.1 デスクトップ版

#### 5.1.0 トップ画面（2つの独立ボタン）

トップ画面に2つのメイン機能ボタンを配置：

```
┌─────────────────────────────────────────────────────────┐
│  🎒 通学路安全確認                                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────┐  ┌─────────────────────┐     │
│  │  📍                  │  │  🚶                  │     │
│  │  マイ危険ポイント設置        │  │  探検モード          │     │
│  │                      │  │                      │     │
│  │  危険だと思う場所に   │  │  通学路をStreet View │     │
│  │  ピンを立てよう       │  │  で歩いてみよう      │     │
│  │                      │  │                      │     │
│  │  設置済み: 5件       │  │                      │     │
│  └─────────────────────┘  └─────────────────────┘     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 5.1.1 マイ危険ポイント設置画面

```
┌─────────────────────────────────────────────────────────┐
│  ピン設置フェーズ                     [ツアーを開始 →]  │
├─────────────────────────────┬───────────────────────────┤
│                             │                           │
│     地図表示エリア           │    ガイドパネル           │
│     ・経路表示（青線）       │                           │
│     ・HazardPoint（既存）   │    📍 設置したピン: 3件    │
│     ・マイ危険ポイント（紫）        │    ─────────────────────  │
│                             │    1. 〇〇交差点付近       │
│     💡 地図をタップして      │       見通しが悪い        │
│        ピンを設置           │    2. △△通り             │
│                             │       道路が狭い/         │
│                             │       スピードが出る車    │
│                             │    3. □□前               │
│                             │       交通量が多い        │
│                             │                           │
│                             │    [編集] [削除]          │
└─────────────────────────────┴───────────────────────────┘
```

#### 5.1.2 理由選択ダイアログ（複数選択）

```
┌─────────────────────────────────────┐
│  🚩 危険な理由を選んでください        │
│     （複数選択できます）              │
├─────────────────────────────────────┤
│  ☑ 道路が狭い/歩道がない            │
│  ☐ 見通しが悪い                     │
│  ☑ スピードを出すクルマが多い        │
│  ☐ 交通量が多い                     │
│  ☐ 歩行者/自転車との接触が心配      │
│  ☐ その他                          │
│    └ [補足を入力...              ]  │
├─────────────────────────────────────┤
│        [キャンセル]  [ピンを立てる]   │
└─────────────────────────────────────┘
```

#### 5.1.3 巡回対象選択画面

経路設定完了後に表示：

```
┌─────────────────────────────────────────────────────────┐
│  経路が設定されました                    [← 経路を変更]  │
├─────────────────────────────┬───────────────────────────┤
│                             │                           │
│     地図表示エリア           │  どの情報でツアーする？   │
│     ・設定した経路（青線）   │                           │
│     ・HazardPoint表示       │  ○ 自分で立てたピン       │
│     ・マイ危険ポイント表示          │     (マイ危険ポイント)            │
│                             │     設置済み: 5件         │
│                             │                           │
│                             │  ○ Safety Mapの投稿情報  │
│                             │     この経路で4地点を選別  │
│                             │                           │
│                             │  ─────────────────────    │
│                             │                           │
│                             │  [ツアーを開始]           │
└─────────────────────────────┴───────────────────────────┘
```

#### 5.1.4 ツアー中（マイ危険ポイント停止時）

```
┌─────────────────────────────────────────────────────────┐
│  ツアーコントロールバー                                   │
│  [◀◀][▶/⏸][▶▶]  進捗バー                              │
├─────────────────────────────┬───────────────────────────┤
│                             │                           │
│     地図表示エリア           │    Street View エリア     │
│     ・経路表示              │                           │
│     ・現在位置マーカー       │                           │
│                             ├───────────────────────────┤
│                             │   あなたが立てたピン 🚩    │
│                             │   ─────────────────────   │
│                             │   選んだ理由:             │
│                             │   ・道路が狭い/歩道がない  │
│                             │   ・スピードを出す車が多い │
│                             │                           │
│                             │   [確認OK → 次へ進む]     │
└─────────────────────────────┴───────────────────────────┘
```

### 5.2 モバイル版

#### 5.2.0 トップ画面（2つの独立ボタン）

```
┌─────────────────────────────────────┐
│  🎒 通学路安全確認                  │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │  📍 マイ危険ポイント設置             │   │
│  │  危険だと思う場所にピンを立てよう │   │
│  │  設置済み: 5件               │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  🚶 探検モード               │   │
│  │  通学路をStreet Viewで歩いてみよう │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

#### 5.2.1 マイ危険ポイント設置画面

- 地図全画面表示
- 上部に「地図をタップしてピンを設置」のガイド表示
- 地図タップで理由選択ボトムシート表示
- 下部に設置済みピン数と完了ボタン

#### 5.2.2 巡回対象選択（ボトムシート）

経路設定完了後に表示：

```
┌─────────────────────────────────────┐
│  ─── （ドラッグハンドル）            │
│                                     │
│  どの情報でツアーしますか？          │
│                                     │
│  ○ 自分で立てたピン（5件）          │
│  ○ Safety Mapの投稿情報（4地点選別） │
│                                     │
│  [ツアーを開始]                      │
└─────────────────────────────────────┘
```

#### 5.2.3 理由選択ボトムシート

```
┌─────────────────────────────────────┐
│  ─── （ドラッグハンドル）            │
│                                     │
│  🚩 危険な理由を選んでください        │
│                                     │
│  ☑ 道路が狭い/歩道がない            │
│  ☐ 見通しが悪い                     │
│  ☑ スピードを出すクルマが多い        │
│  ...                                │
│                                     │
│  [ピンを立てる]                      │
└─────────────────────────────────────┘
```

#### 5.2.3 マイ危険ポイント停止時

- 通常のHazardStop画面と同様のレイアウト
- 「あなたが立てたピン」として表示
- 選んだ理由を一覧表示

---

## 6. 実装計画

### Phase 1: データ層

- [ ] MyHazardPoint型定義の追加（lib/types.ts）
- [ ] MyHazardReason enum定義
- [ ] TourTarget型定義（'my_hazard_points' | 'safety_map'）
- [ ] ローカルストレージ管理Hook（lib/useMyHazardPoints.ts）
- [ ] CRUD操作の実装（追加、編集、削除、一覧取得）

### Phase 2: Safety Map自動選別ロジック

- [ ] HazardPoint選別関数（lib/hazardData.ts拡張）
- [ ] 優先度スコア計算ロジック
- [ ] 上位3〜5件選出ロジック
- [ ] 近接地点の重複除去

### Phase 3: 状態管理

- [ ] マイ危険ポイント設置モード用Hook（lib/useMyHazardPointMode.ts）
  - `pin_idle`: 初期状態
  - `pin_editing`: 編集中
- [ ] useExplorationMode.tsに`target_select`フェーズ追加
- [ ] 巡回対象（tourTarget）の状態管理
- [ ] フェーズ遷移ロジックの実装

### Phase 4: 地図表示

- [ ] マイ危険ポイントマーカーコンポーネント（MyHazardMarkers.tsx）
- [ ] ピンのスタイル（紫色、アイコン）
- [ ] 選択時のポップアップ表示
- [ ] 重複防止ロジック（30m以内の警告）

### Phase 5: UI - トップ画面・巡回対象選択（デスクトップ）

- [ ] トップ画面に「マイ危険ポイント設置」「探検モード」ボタン追加
- [ ] 巡回対象選択パネル
- [ ] Safety Map選別結果プレビュー

### Phase 6: UI - ピン設置（デスクトップ）

- [ ] ピン設置モード用パネル
- [ ] 理由選択ダイアログ（複数選択対応）
- [ ] 設置済みピン一覧表示
- [ ] 編集・削除機能

### Phase 7: UI - モバイル対応

- [ ] トップ画面に「マイ危険ポイント設置」「探検モード」ボタン追加（モバイル）
- [ ] マイ危険ポイント設置画面（モバイル）
- [ ] 理由選択ボトムシート
- [ ] 巡回対象選択ボトムシート

### Phase 8: ツアー連携

- [ ] 巡回対象に応じた停止地点リスト生成
- [ ] マイ危険ポイント停止時の表示パネル
- [ ] HazardPoint停止時の表示（既存活用）

### Phase 9: 完了画面連携

- [ ] CertificateModalへの巡回情報追加
- [ ] 設置ピン数・理由サマリー表示（マイ危険ポイント使用時）
- [ ] 確認地点サマリー表示（Safety Map使用時）

---

## 7. 決定事項と未決定事項

### 7.1 決定済み

| 項目 | 決定内容 |
|------|---------|
| 機能構成 | 「マイ危険ポイント設置」と「探検モード」を独立したボタンで配置 |
| 巡回対象選択 | 探検モード開始時にマイ危険ポイント or Safety Mapを選択 |
| Safety Map選別 | 経路上のHazardPointを3〜5件に自動選別 |
| 理由選択方式 | 複数選択（チェックボックス） |
| HazardPointとの紐付け | デモ版では紐付けなし |
| データ永続化 | localStorage |

### 7.2 未決定事項

| 項目 | 選択肢 | 現在の方針 |
|------|--------|-----------|
| 重複防止の距離閾値 | 10m / 20m / 30m / 50m | 30m（要検証） |
| ピン設置上限 | なし / 10件 / 20件 | なし（デモ版） |
| Safety Map選別件数 | 3件 / 4件 / 5件 / 可変 | 3〜5件（可変） |
| 選別アルゴリズムの重み | 調整可能 | 要検証 |
| マイ危険ポイント0件時のツアー | マイ危険ポイント選択不可 / Safety Mapのみ | Safety Mapのみ選択可 |

---

## 8. 将来拡張（参考）

以下は将来版で検討する機能：

### 8.1 サーバー連携アーキテクチャ

#### 8.1.1 基本方針

アカウント作成なしで、以下の両立を実現する：
- **ローカル**: 個人のマイ危険ポイントを永続化（いつでも確認可能）
- **サーバー**: 匿名化されたデータを蓄積（分析・精度向上に活用）

#### 8.1.2 匿名ID方式

```
┌─────────────────────────────────────────────────────────┐
│  ユーザーのブラウザ                                      │
│                                                         │
│  localStorage / IndexedDB                               │
│  ├── anonymous_id: "uuid-xxx-yyy"  ← 初回訪問時に生成   │
│  └── my_hazard_points: [...]       ← マイ危険ポイント   │
│                                                         │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ 匿名化データ送信
                        │ - anonymous_id（デバイス識別、個人特定不可）
                        │ - 位置情報（lat, lng）
                        │ - 危険理由
                        │ - 学校ID（任意）
                        ▼
┌─────────────────────────────────────────────────────────┐
│  サーバー                                                │
│                                                         │
│  匿名化マイ危険ポイント集計DB                            │
│  ├── 投稿情報（HazardPoint）との照合・検証               │
│  ├── 地域別・学校別の危険地点ヒートマップ生成            │
│  └── ナビ配信向け「検証済み」情報のフィルタリング        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 8.1.3 データ永続化の役割分担

| 保存先 | 目的 | 内容 |
|--------|------|------|
| **localStorage** | 個人用 | マイ危険ポイント全データ、anonymous_id |
| **サーバー** | 分析用 | 匿名化されたピン情報、集計データ |

#### 8.1.4 動作フロー

```
【初回訪問】
1. anonymous_id を生成して localStorage に保存
2. マイ危険ポイントを設置
   → localStorage に保存（即座に）
   → サーバーに匿名送信（バックグラウンド）

【再訪問】
1. localStorage から anonymous_id を取得
2. localStorage からマイ危険ポイントを復元
   → 以前設置したピンがそのまま表示される

【オフライン時】
1. localStorage にのみ保存
2. オンライン復帰時にサーバーへ同期
```

#### 8.1.5 プライバシー配慮

| 項目 | 方針 |
|------|------|
| 個人情報 | 一切取得しない |
| anonymous_id | デバイス識別用のみ。個人特定不可 |
| 位置情報 | ピン位置のみ。ユーザーの現在地は取得しない |
| データ削除 | ユーザーがブラウザデータをクリアすれば完全削除 |

#### 8.1.6 データ構造（サーバー送信用）

```typescript
// サーバー送信用（匿名化済み）
interface AnonymousHazardPoint {
  anonymous_id: string;          // デバイス識別子（個人特定不可）
  lat: number;                   // 緯度
  lng: number;                   // 経度
  reasons: MyHazardReason[];     // 危険理由
  school_id?: string;            // 学校ID（任意）
  created_at: string;            // 作成日時
}
```

#### 8.1.7 注意事項

| 状況 | 結果 |
|------|------|
| ブラウザを閉じて再訪問 | ローカルデータは残る |
| プライベートブラウズモード | セッション終了で消える |
| ブラウザのデータクリア | ローカルデータは消える（サーバー側は残る） |
| 別デバイスで訪問 | 同期されない（アカウントがないため） |

### 8.2 認証・学校管理

- 学校ID + パスワードによるアクセス制御
- 学校ごとのマイ危険ポイント集計
- 学校単位でのデータエクスポート

### 8.3 投稿情報マッチング

- マイ危険ポイントとHazardPointの位置・理由マッチング
- 「検証済み」フラグの付与
- ナビ配信向けフィルタリング

### 8.4 分析ダッシュボード

- 学校・自治体向けのデータ可視化
- ヒートマップ表示
- 時系列分析

---

## 更新履歴

| 日付 | バージョン | 内容 |
|------|-----------|------|
| 2026-01-23 | v0.1 | 初版作成（SPEC_MSRH_PIN.md） |
| 2026-01-23 | v0.2 | デモ版として再構成、既存アプリとの統合方針追加 |
| 2026-01-23 | v0.3 | ピン設置とツアーを独立機能に変更、Safety Map自動選別ロジック追加 |
| 2026-01-23 | v0.4 | 「MSRHピン設置」と「探検モード」をトップレベルの独立ボタンに変更 |
| 2026-01-23 | v0.5 | 「MSRHピン」を「マイ危険ポイント」に名称変更 |
| 2026-01-23 | v0.6 | 将来のサーバー連携アーキテクチャを追加 |
